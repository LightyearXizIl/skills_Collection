interface:
  display_name: "Prototype UI Variants"
  short_description: "Build and compare distinct live UI variants"
  default_prompt: "Use $prototype to build several genuinely different live variants of this UI and present them in a visual picker."
policy:
  allow_implicit_invocation: false
