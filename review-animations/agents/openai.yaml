interface:
  display_name: "Review Animations"
  short_description: "Review motion code against a strict craft bar"
  default_prompt: "Use $review-animations to review this animation code against a strict design engineering standard."
policy:
  allow_implicit_invocation: false
